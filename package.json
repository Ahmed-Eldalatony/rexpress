{
  "name": "react-express-ts-vite-template",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node --watch --experimental-strip-types server/index.ts",
    "start": " NODE_ENV=production  node --experimental-strip-types server/index.ts",
    "build": "cd client && npm run build:client &&  npm run build:server"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0",
    "@types/express": "^4.18.2",
    "vite": "^4.0.0"
  }
}
