{
  "name": "react-express-ts-vite-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only server/index.ts",
    "build:client": "cd client && npm run build:client &&  npm run build:server",
    "build:server": "tsc -p server/index.ts --outDir dist --skipLibCheck",
    "build": "npm run build:client && npm run build:server",
    "start": "node ./dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.0",
    "@types/express": "^4.18.2",
    "vite": "^4.0.0"
  }
}
